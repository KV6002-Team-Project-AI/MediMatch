/*!
 * Built with Duet Design System
 */
import { createPage } from "../../utils/test-utils";
describe("duet-badge", () => {
    it("should render a badge", async () => {
        const page = await createPage(`<duet-badge>Test</duet-badge>`);
        const component = await page.find("duet-badge");
        expect(component).not.toBeNull();
        expect(component).toHaveClass("hydrated");
    });
    it("should provide a property that removes the margin of the component", async () => {
        const page = await createPage(`<duet-badge margin="none">
    </duet-badge>`);
        const element = await page.find("duet-badge");
        expect(element).toHaveClass("duet-m-0");
    });
    it("should provide a property that renders badge in different style variations", async () => {
        const page = await createPage(`<duet-badge variation="danger">Test</duet-badge>`);
        const element = await page.find("duet-badge >>> .duet-badge");
        expect(element).toHaveClass("danger");
    });
    it("should provide a property that switches the theme", async () => {
        const page = await createPage(`<duet-badge theme="turva">Test</duet-badge>`);
        const element = await page.find("duet-badge >>> .duet-badge");
        expect(element).toHaveClass("duet-theme-turva");
    });
});
