/*!
 * Built with Duet Design System
 */
import { createPage } from "../../utils/test-utils";
describe("duet-banner", () => {
    it("should render a badge", async () => {
        const page = await createPage(`<duet-banner>Test</duet-banner>`);
        const component = await page.find("duet-banner");
        expect(component).not.toBeNull();
        expect(component).toHaveClass("hydrated");
    });
    it("should provide a property that removes the margin of the component", async () => {
        const page = await createPage(`<duet-banner margin="none">
    </duet-banner>`);
        const element = await page.find("duet-banner");
        expect(element).toHaveClass("duet-m-0");
    });
    it("should provide a property that switches the theme", async () => {
        const page = await createPage(`<duet-banner theme="turva">Test</duet-banner>`);
        const element = await page.find("duet-banner >>> .duet-banner");
        expect(element).toHaveClass("duet-theme-turva");
    });
});
