/*!
 * Built with Duet Design System
 */
import { formatViolations, options as axeRunOptions, testWithAxe } from "../../utils/axe-core-utils";
describe("[a11y] duet-breadcrumbs", () => {
    it("should render an accessible component", async () => {
        const result = await testWithAxe(`
        <duet-breadcrumbs class="duet-example-without-padding">
          <duet-breadcrumb href="/" icon="damage-home" accessible-label="Etusivu"></duet-breadcrumb>
          <duet-breadcrumb href="/edut-ja-kampanjat">Edut ja kampanjat</duet-breadcrumb>
          <duet-breadcrumb>LähiTapiola Pirkanmaa</duet-breadcrumb>
        </duet-breadcrumbs>
      `, axeRunOptions);
        expect(result).not.toBeNull();
        expect(formatViolations(result.violations)).toEqual([]);
    });
});
