/*!
 * Built with Duet Design System
 */
import { createPage } from "../../utils/test-utils";
describe("duet-breadcrumbs", () => {
    it("should render", async () => {
        const page = await createPage(`
    <duet-breadcrumbs>
      <duet-breadcrumb href="/" icon="damage-home" accessible-label="Etusivu"></duet-breadcrumb>
      <duet-breadcrumb href="/edut-ja-kampanjat">Edut ja kampanjat</duet-breadcrumb>
      <duet-breadcrumb href="/edut-ja-kampanjat/pirkanmaa">LähiTapiola Pirkanmaa</duet-breadcrumb>
    </duet-breadcrumbs>
    `);
        const component = await page.find("duet-breadcrumbs");
        expect(component).not.toBeNull();
        expect(component).toHaveClass("hydrated");
    });
});
