/*!
 * Built with Duet Design System
 */
import { getAllFixturesFiles } from "../../utils/fixture-utils";
import { createPage } from "../../utils/test-utils";
const { fixtures } = getAllFixturesFiles(__dirname);
describe("duet-fieldset", () => {
    it("should render an empty state", async () => {
        const page = await createPage(fixtures.basic_fieldset);
        const component = await page.find("duet-fieldset");
        expect(component).not.toBeNull();
    });
});
