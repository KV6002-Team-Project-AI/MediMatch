/*!
 * Built with Duet Design System
 */
import { createPage } from "../../utils/test-utils";
describe("duet-heading", () => {
    it("should render a heading", async () => {
        const page = await createPage(`<duet-heading></duet-heading>`);
        const component = await page.find("duet-heading");
        expect(component).not.toBeNull();
        expect(component).toHaveClass("hydrated");
    });
    it("should provide a property that switches the theme", async () => {
        const page = await createPage(`<duet-heading theme="turva"></duet-heading>`);
        const element = await page.find("duet-heading >>> .duet-heading");
        expect(element).toHaveClass("duet-theme-turva");
    });
    it("should provide a property that removes the margin of the component", async () => {
        const page = await createPage(`<duet-heading margin="none"></duet-heading>`);
        const element = await page.find("duet-heading >>> .duet-m-0");
        expect(element).not.toBeNull();
    });
    it("should provide a property that renders different heading levels", async () => {
        const page = await createPage(`<duet-heading level="h4"></duet-heading>`);
        const element = await page.find("duet-heading >>> h4");
        expect(element).not.toBeNull();
    });
});
