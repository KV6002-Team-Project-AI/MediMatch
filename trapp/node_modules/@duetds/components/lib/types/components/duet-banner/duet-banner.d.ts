import { DuetColor, DuetHeadingLevel, DuetIconName, DuetMargin, DuetTheme } from "../../common-types";
import { ThemeableComponent } from "../../common/themeable-component";
import { DuetSource } from "../../utils/picture-utils";
export declare class DuetBanner implements ThemeableComponent {
    private hasHeadingSlot;
    private hasDescriptionSlot;
    private hasActionSlot;
    element: HTMLElement;
    /**
     * Theme.
     */
    theme: DuetTheme;
    /**
     * Controls the margin of the component.
     */
    margin: DuetMargin;
    /**
     * Background color.
     */
    backgroundColor: DuetColor;
    /**
     * Icon color.
     */
    iconColor: DuetColor;
    /**
     * Icon background color.
     */
    iconBackgroundColor: DuetColor;
    /**
     * Text color.
     */
    textColor: DuetColor;
    /**
     * Size.
     */
    size: "auto" | "small" | "large";
    /**
     * Image. Example image is 512x341;
     */
    image: string;
    /**
     * Array of sources for responsive images. Sources have the properties of <source> element.
     * This is an experimental feature and might be changed without notice.
     */
    sources: DuetSource[] | string;
    /**
     * Icon.
     */
    icon: DuetIconName;
    /**
     * The actual heading level used in the HTML markup.
     */
    headingLevel: DuetHeadingLevel;
    /**
     * Component lifecycle events.
     */
    componentWillLoad(): void;
    /**
     * render() function
     * Always the last one of the class.
     */
    render(): any;
}
