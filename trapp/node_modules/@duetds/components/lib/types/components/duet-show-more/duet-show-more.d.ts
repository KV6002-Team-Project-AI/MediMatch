import { EventEmitter } from "../../stencil-public-runtime";
import { DuetLanguage, DuetMargin, DuetPadding, DuetTheme } from "../../common-types";
import { ThemeableComponent } from "../../common/themeable-component";
import { DuetLangObject } from "../../utils/language-utils";
export type DuetShowMoreToggleEvent = {
    component: "duet-show-more";
    originalEvent: KeyboardEvent | MouseEvent;
};
export declare class DuetShowMore implements ThemeableComponent {
    private contentElement;
    private contentId;
    private buttonId;
    element: HTMLElement;
    buttonAriaOpen: boolean;
    /**
     * Theme.
     */
    theme: DuetTheme;
    /**
     * The currently active language. This setting also changes the logo to match
     * the chosen language.
     * @deprecated this is now handled via the html lang tag, and is no longer used - kept to avoid breaking changes and ease unit testing
     * @default "fi"
     */
    language: DuetLanguage;
    /**
     * Default translations for show more label.
     */
    showMoreLabelDefaults: DuetLangObject;
    /**
     * Show more label. Defaults to what is in showMoreLabelDefaults prop.
     */
    showMoreLabel: string;
    /**
     * Default translations for show less label.
     */
    showLessLabelDefaults: DuetLangObject;
    /**
     * Show less label. Defaults to what is in showLessLabelDefaults prop.
     */
    showLessLabel: string;
    /**
     * Default translations for show more accessible label.
     */
    showMoreAccessibleLabelDefaults: DuetLangObject;
    /**
     * Show more accessible label. Defaults to what is in showMoreAccessibleLabelDefaults prop.
     */
    showMoreAccessibleLabel: string;
    /**
     * Default translations for show less accessible label.
     */
    showLessAccessibleLabelDefaults: DuetLangObject;
    /**
     * Show less accessible label. Defaults to what is in showLessAccessibleLabelDefaults prop.
     */
    showLessAccessibleLabel: string;
    /**
     * Controls the margin of the component.
     */
    margin: DuetMargin;
    /**
     * Controls the margin of the button component inside of show more.
     */
    buttonMargin: DuetMargin;
    /**
     * Controls the padding of the button component inside of show more.
     */
    buttonPadding: DuetPadding;
    /**
     * Is the component expanded or collapsed.
     */
    open: boolean;
    /**
     * Event emitted when opened/closed
     */
    duetToggle: EventEmitter<DuetShowMoreToggleEvent>;
    /**
     * Component lifecycle events.
     */
    componentWillLoad(): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    private handleKeyDown;
    private handleClick;
    /**
     * render() function
     * Always the last one of the class.
     */
    render(): any;
}
